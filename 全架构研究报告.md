# yuai-gui 全架构研究报告

> 完整代码审查 + 差距分析 + 修复方案
> 2026-06-26

---

## 一、架构总览

```
技术栈: Tauri v2 + Vue 3.5 + Pinia 3 + Vite 5 + Rust
身份: cn.yuai-r.gui — "Yuai · 与 AI · 同在"
规模: 63 个文件, ~247KB 代码, 44 个 Tauri 命令
```

### Rust 后端 (19 模块)

| 模块 | 行数 | 职责 |
|------|------|------|
| lib.rs | 192 | 入口, AgentDef 类型, 44 命令注册 |
| pty.rs | 390 | PTY 管理, Agent 启动, 配置注入 |
| bus.rs | 523 | 群聊引擎, @mention, 收敛检测 |
| agents.rs | 174 | Agent 定义, 二进制路径解析 |
| files.rs | 249 | 文件树, 读写, 原子写入 |
| config.rs | 193 | Provider 配置 (SQLite) |
| context.rs | 205 | 上下文压缩 (CJK Token 估算) |
| watcher.rs | 155 | 文件监控 (notify crate) |
| recording.rs | 256 | 终端录制 (asciinema v2) |
| screenshot.rs | 230 | 截图检测 |
| updater.rs | 93 | 自动更新 |
| disk.rs | 100 | 磁盘占用 |
| window.rs | 88 | 窗口状态持久化 |
| git.rs | 99 | Git 操作 |
| chat_db.rs | 78 | 聊天持久化 |
| commands.rs | 99 | 群聊命令桥接 |
| tools.rs | 76 | 工具描述 (死代码) |
| thumbnail.rs | 144 | 缩略图生成 |
| main.rs | 6 | Windows 子系统属性 |

### 前端 (21 组件 + 9 stores + 6 composables)

| 组件 | 职责 |
|------|------|
| ChatPanel | 群聊/单聊/Beam UI |
| SettingsPanel | API 配置 |
| Terminal | xterm.js PTY 终端 |
| DiffViewer | Git Diff |
| BeamPanel | 并行提问 |
| WechatPanel | 微信 ClawBot |
| MonacoEditor | 代码编辑器 |
| CommandPalette | ⌘K 命令面板 |
| ScreenshotToast | 截图通知 |
| 更多... | |

---

## 二、已知 Bug

### Critical (5 个)

| # | Bug | 影响 |
|---|-----|------|
| 1 | PTY session 泄漏 — 进程退出后 session 仍在 HashMap | 内存泄漏 |
| 2 | sendMessage 始终用 group_send — 单聊模式无效 | 单聊不工作 |
| 3 | workspacePath 非响应式 — `let` 变量不会触发更新 | 状态陈旧 |
| 4 | BeamPanel 未在 App.vue 渲染 | Beam 模式不显示 |
| 5 | confirm_execution 不真正执行 — 只设 phase 不运行 | 执行流断裂 |

### Moderate (5 个)

| # | Bug | 影响 |
|---|-----|------|
| 6 | tools.rs 死代码 | 代码膨胀 |
| 7 | compress_context 返回 prompt 不是摘要 | 压缩无效 |
| 8 | 重复 CSS (ChatPanel scoped vs global) | 样式冲突 |
| 9 | Monaco 编辑器未接入布局 | 代码预览弱 |
| 10 | 无 CI/CD | 无自动构建 |

---

## 三、与 FanBox 差距

| 功能 | FanBox | yuai-gui | 差距 |
|------|--------|----------|------|
| Agent 启动 | 登录 shell PATH 查找 | 硬编码 bundle 路径 | **大** — openclaw/hermes 不工作 |
| 配置注入 | 全局配置 (~/.claude/) | 环境变量注入 | **中** — 简化版 |
| 文件搜索 | 模糊搜索 + Spotlight + grep | 无 | **大** |
| Monaco 编辑器 | 未使用 | 组件存在但未接入 | **中** |
| 执行流程 | 完整 discuss→execute | 只有 discuss | **大** |

## 四、与 Hermes Studio 差距

| 功能 | Hermes Studio | yuai-gui | 差距 |
|------|--------------|----------|------|
| Agent 启动 | 代理服务器 + scoped 配置 | 直接 env 注入 | **中** |
| 上下文压缩 | LLM 实际压缩 | 只生成 prompt | **大** |
| 工作流引擎 | DAG 拓扑执行 | 无 | **大** |
| Provider 预设 | 内置配置 | 手动配置 | **中** |
| 测试套件 | Vitest 全覆盖 | 无测试 | **大** |

---

## 五、修复优先级

### P0 — 必须修复

1. **单聊模式** — 跳过 group_send，直接 spawn agent 对话
2. **BeamPanel 渲染** — 接入 App.vue
3. **执行流** — confirmExecution 真正运行 agent
4. **Agent 启动** — 用登录 shell PATH 查找（参考 FanBox）

### P1 — 重要

5. **CI/CD** — GitHub Actions 自动构建
6. **测试套件** — Vitest + cargo test
7. **上下文压缩** — 实际调用 LLM
8. **文件搜索** — ripgrep 后端
9. **Provider 预设** — 内置配置
10. **Monaco 编辑器** — 接入代码预览

### P2 — 增强

11. 工作流引擎
12. 写入审批
13. 多平台 bundle
14. 日志系统
