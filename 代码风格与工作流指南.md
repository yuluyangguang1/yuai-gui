# yuai-gui 代码风格与工作流程指南

> 基于 `/Users/ylyg/Desktop/yuai-gui` 最新代码归纳，用于 Phase 5 及后续开发参照。

---

## 一、前端代码风格

### 1.1 技术栈锚点
- **Vue 3.5** + **TypeScript 6.0** + **Pinia** + **Vite 5.4**
- **Tauri 2.x**（桌面壳层，Rust 后端）
- **Vitest 4.1** 作为测试框架

### 1.2 组件组织模式

```
src/
├── main.ts              # 极简引导：createApp → use(Pinia) → mount
├── App.vue              # 根组件，职责：布局骨架 + previewMode 切换
├── components/
│   ├── layout/          # AppTitlebar / AppRail / AppStatusbar
│   ├── panels/          # ChatPanel / Terminal / DiffViewer / SettingsPanel
│   ├── views/           # HomeView / PreviewView
│   └── util/            # CommandPalette / ToastContainer
├── stores/              # Pinia Setup Stores（见 1.3）
├── composables/         # 可复用组合式函数
└── styles/
    ├── variables.css    # 全局 CSS 变量（见 1.4）
    ├── design-system.css # 设计令牌
    └── ...
```

**根组件原则：**
- 使用 `<script setup lang="ts">`
- 重型组件（`Terminal`、`WechatPanel`、`WorkflowEditor`）用 `defineAsyncComponent()` 异步加载
- 在根层一次性实例化 stores，状态与 UI 权责分离
- 布局：左侧 Rail + 右侧 `preview-col`，通过 `previewMode` 字符串枚举控制面板切换（`v-if / v-else-if` 链）

### 1.3 Pinia Store 模式

**标准范式：**
```ts
// ✅ 使用 Setup Store
export const useChatStore = defineStore("chat", () => {
  // 状态
  const messages = ref<Message[]>([])
  // 计算
  const unreadCount = computed(() => ...)
  // 监听
  watch(messages, () => ...)
  // 方法直接返回
  function sendMessage(content: string) { ... }
  return { messages, unreadCount, sendMessage }
})
```

**关键特点：**
- 先声明领域接口类型（`ChatPhase`、`StreamingMessage`、`ThreadInfo`），再由 `ref/computed` 派生状态
- 跨 Store 协作：直接 `const agentsStore = useAgentsStore()`，不做事件广播
- 线程分组：`Map<string, ThreadInfo>` + `currentThreadId` 追踪，支持父子线程导航
- 流式消息：`streamingMessage` ref 暂存生成内容，timer 控制稳定性
- 持久化委托：在 `addMessage` 中检测 `workspacePath` 存在后自动调用 Tauri `invoke("save_chat_message")`

### 1.4 CSS 变量命名规范

**分层语义：**
```
--bg-*          # 背景层级 primary / secondary / tertiary / surface / elevated / hover
--text-*        # 文本层级 primary / secondary / muted / inverse
--accent-*      # 品牌色 accent / hover / muted
--border-*      # 轮廓 border / border-light / border-accent
--shadow-*      # 阴影 sm / md / lg
--radius-*      # 圆角 sm / md / lg
--font-*        # 字体 serif / body / brush / mono
--transition-*  # 动效 fast / normal / slow
```

**规则：**
- 全小写 + 连字符（kebab-case）
- 语义化前缀一眼可辨用途
- 类型化尺寸隐含层级关系（`--bg-tertiary` 比 `--bg-secondary` 更深）
- 别名层：`--jade`（= accent）、`--silver`（= text-muted）、`--bone`（= text-primary）
- **严禁**在组件 scoped CSS 中硬编码颜色值，必须使用 `variables.css` 定义的变量

### 1.5 TypeScript 配置

| 选项 | 值 | 说明 |
|---|---|---|
| `strict` | **true** | 开启全部严格检查 |
| `noUnusedLocals` | false | 允许局部未使用（开发友好） |
| `noUnusedParameters` | false | 允许参数未使用 |
| `baseUrl` / `paths` | `.` / `@/* → ./src/*` | 支持 `@/` 绝对路径别名 |

**惯例：**
- 所有 Store 的 `ref<T>` 必须显式标注类型
- 不引入 `any`（除非极端场景并注释说明）
- 无黑魔法放宽严格模式

---

## 二、Rust 后端代码风格

### 2.1 Tauri Command 注册

```rust
// ① 宏标记
#[tauri::command]
fn spawn_agent(app: tauri::AppHandle, ...) -> Result<String, String> { ... }

// ② mod 声明
mod agents;
mod chat_db;
mod pty;

// ③ 集中注册
tauri::generate_handler![
    spawn_agent,
    pty_write,
    ...
]
```

### 2.2 错误处理风格

- **全部兜底为 `String`**：`Result<T, String>`
- 转换方式：`.map_err(|e| e.to_string())?` 或 `.map_err(|e| format!(...))?`
- 错误信息使用中文，前端可直接显示（如 "连接失败", "agents.json 解析错误"）
- **无自定义错误枚举，无分层包装，无上下文链** —— 轻量到"可用即可"

### 2.3 SQLite 使用方式

- **库**：`rusqlite`
- **位置**：`bundle_root/data/chat_history.db`
- **模式**：每次都 `create_dir_all` → `Connection::open`，无连接池
- **建表**：写入时用 `execute_batch` 执行 `CREATE TABLE IF NOT EXISTS`
- **读写**：写入用默认连接；读取用 `OpenFlags::SQLITE_OPEN_READ_ONLY`
- **查询**：`prepare` + `query_map` + `params!`，循环中用 `if let Ok(r) = row` 静默过滤

### 2.4 模块组织结构

```
src-tauri/src/
├── lib.rs          # 入口 + 41 个命令注册
├── main.rs         # 入口
├── pty.rs          # PTY 管理
├── files.rs        # 文件树
├── chat_db.rs      # SQLite 聊天历史
├── commands.rs     # 群聊命令
├── bus.rs          # 群聊引擎
├── context.rs      # 上下文压缩
├── agents.rs       # Agent 定义（梅兰竹菊莲）
├── config.rs       # Provider 配置
├── recording.rs    # asciinema v2 录制
├── disk.rs         # 磁盘占用（纯 Rust 递归）
├── screenshot.rs   # 截图检测（cfg 分支跨平台）
└── thumbnail.rs    # 缩略图生成
```

---

## 三、构建与工作流程

### 3.1 NPM 脚本

| 脚本 | 命令 | 用途 |
|---|---|---|
| `dev` | `vite` | 开发模式（Tauri 自动集成） |
| `build` | `vite build` | 纯净前端构建 |
| `preview` | `vite preview` | 预览构建产物 |
| `tauri` | `tauri` | 调用 Tauri CLI（桌面打包） |
| `test` | `vitest run` | 一次性测试 |
| `test:watch` | `vitest` | 监听模式测试 |

### 3.2 测试约定

- **框架**：Vitest + jsdom
- **约定路径**：`src/**/__tests__/**/*.test.ts`
- **运行**：`npm test` 或 `npm run test:watch`
- **CI**：`.github/workflows/build.yml` — 仅构建，**无自动化测试步骤**（重要）

### 3.3 CI/Release 流程

**`.github/workflows/build.yml`：**
- 触发：`workflow_dispatch` 或 push tag（`v*`）
- 矩阵：`windows-latest` / `macos-latest (aarch64)` / `macos-latest (x86_64)`
- 产物：平台安装包（`.exe/.msi/.dmg/.app`）
- 无测试步骤

**`.github/workflows/release.yml`：**
1. `bundle`：同 `build.yml` 构建 Tauri 安装包
2. 从 GitHub Release 下载外部 Agent 二进制包（codex、cc-switch、claude、openclaw、hermes、openhuman）
3. 组装到 `bundle/` 目录 + `data/agents.json`
4. 打包成平台 zip 上传 artifact
5. `release` 阶段：tag push 时自动创建 GitHub Release

### 3.4 Commit 约定

**未配置 commitlint / husky / .gitmessage 等强制工具。**
- 团队通过文档或口头约定约束
- 实际提交示例：`feat: Phase 4 — 4 个功能实现 (15 个项目研究整合)`

### 3.5 重要文件/目录

- `Cargo.toml`（Rust 依赖，`Cargo.lock` 被 `.gitignore` 排除）
- `tauri.conf.json`：`devUrl: "http://localhost:1420"`，`frontendDist: "../dist"`
- `vite.config.ts`：Vite 5 + Vue 插件 + manualChunks 代码分割
- `package.json`：依赖版本冻结，使用 `pnpm-lock.yaml` 或 `package-lock.json`

### 3.6 开发工作流

```bash
# 1. 安装后首次初始化外部 bundle（已有脚本）
./scripts/setup-bundle.sh

# 2. 开发模式
npm run dev        # 启动 Vite dev server
npm run tauri dev  # 启动 Tauri 桌面应用（热重载）

# 3. 前端构建验证
npm run build      # Vite 构建到 dist/

# 4. Tauri 桌面构建
npm run tauri build

# 5. 测试
npm test           # 一次性
npm run test:watch # 监听模式
```

---

## 四、后续 Phase 5 开发要点

1. **新功能代码位置**：
   - 新 Rust 模块 → `src-tauri/src/<module>.rs` + 在 `lib.rs` 中 `mod` 声明 + `generate_handler!` 注册
   - 新 Vue 组件 → `src/components/<Feature>.vue` + 在 `App.vue` 中异步加载引入
   - 新 Store → `src/stores/<feature>.ts`

2. **CSS 变更**：
   - 只在 `src/styles/variables.css` 增加/修改变量
   - 组件内使用 `var(--bg-...)`，禁止硬编码颜色

3. **错误处理**：
   - Rust 端统一 `Result<T, String>`
   - 前端用 `try { ... } catch (e) { console.error(e); /* 显示中文提示 */ }`

4. **构建验证**：
   - 每次完成功能先跑 `npm run build` 验证前端编译
   - 再跑 `npm run tauri dev` 运行桌面应用

---

*生成时间：2026-06-28*
*适用项目：/Users/ylyg/Desktop/yuai-gui*
