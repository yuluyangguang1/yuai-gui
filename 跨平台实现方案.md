# yuai-gui 跨平台实现方案

> macOS + Windows 双平台支持
> 2026-06-26

---

# 一、FanBox 平台差异总结

## 1.1 核心差异（14 处）

| 类别 | macOS | Windows | yuai-gui 方案 |
|------|-------|---------|--------------|
| **标题栏** | `hiddenInset` + vibrancy 毛玻璃 | `hidden` + `titleBarOverlay` | Tauri 原生标题栏（自动适配） |
| **睡眠阻止** | `pmset disablesleep` + sudoers | 未实现 | 暂不需要 |
| **截图目录** | `defaults read com.apple.screencapture` | `Pictures\Screenshots` | `#[cfg]` 分支 |
| **剪贴板** | osascript | PowerShell .NET | Tauri plugin-clipboard |
| **回收站** | osascript → Finder | PowerShell → RecycleBin | `trash` crate |
| **缩略图** | `sips` / `qlmanage` | `sharp` (npm) | `image` crate（纯 Rust） |
| **Shell 检测** | `/bin/zsh -lc command -v` | `cmd /c where` | `#[cfg]` 分支 |
| **目录大小** | `du -sk` | PowerShell Get-ChildItem | 纯 Rust 递归 |
| **打开文件** | `open` / `open -a Terminal` | `start` / `explorer` | `tauri::shell::open` |
| **驱动器** | 单根 `/` | A:-Z: 扫描 | 平台检测 |
| **文件监控** | FSEvents (recursive) | ReadDirectoryChanges | `notify` crate（已用） |
| **终端 CWD** | `lsof` | `lsof` (不可用) | 进程 API |
| **图标** | `.icns` (静态) | `.ico` (可动态) | 平台特定资源 |
| **安装器** | DMG | NSIS | Tauri 自动处理 |

## 1.2 FanBox Windows 做了什么

1. 标题栏：`hidden` + `titleBarOverlay`（Chromium 原生窗口控件）
2. 截图目录：`Pictures\Screenshots` + 添加 `屏幕截图` 命名模式
3. 回收站：PowerShell `Microsoft.VisualBasic.FileIO.FileSystem`
4. 缩略图：添加 `sharp` 依赖
5. Shell：`cmd /c where` 替代 `zsh -lc command -v`
6. 驱器扫描：A:-Z: 检测已挂载驱动器
7. 剪贴板：PowerShell `System.Windows.Forms.Clipboard`
8. 菜单：File 菜单放 Quit，无 Application 菜单
9. 窗口关闭：直接退出（非 macOS 的保持运行）
10. 键盘：移除 ⌘ 模态，使用 Ctrl

---

# 二、yuai-gui 平台审计

## 2.1 已跨平台（无需修改）

| 模块 | 状态 | 原因 |
|------|------|------|
| Tauri 插件 (shell/dialog/fs/process) | ✅ | 全部跨平台 |
| PTY 管理 (portable-pty) | ✅ | Windows 用 ConPTY |
| 二进制解析 (agents.rs/tools.rs) | ✅ | 已有 .exe 后缀 + PATH 分隔符 |
| 文件监控 (notify crate) | ✅ | Windows 用 ReadDirectoryChanges |
| Git 命令 (git.rs) | ✅ | 需要 Git for Windows 在 PATH |
| 数据库 (rusqlite bundled) | ✅ | 从源码编译 SQLite |
| HTTP (reqwest) | ✅ | 跨平台 |
| 图片处理 (image crate) | ✅ | 纯 Rust |
| 键盘快捷键 | ✅ | `metaKey \|\| ctrlKey` |
| Tauri 构建目标 | ✅ | 已有 nsis/msi/icon.ico |
| 所有 npm 依赖 | ✅ | 全部跨平台 |

## 2.2 需要修复（10 处）

### 🔴 Critical（必须修复）

| # | 文件 | 行 | 问题 | 修复 |
|---|------|-----|------|------|
| 1 | screenshot.rs | 49-51 | `defaults read` 仅 macOS | `#[cfg]` 分支，Windows 用 `Pictures\Screenshots` |
| 2 | disk.rs | 72-76 | `du -sk` 不存在于 Windows | 纯 Rust 递归目录遍历 |
| 3 | usePathResolver.ts | 19 | 路径正则只匹配 Unix `/foo` | 添加 Windows `C:\foo` 模式 |
| 4 | useProjectMemory.ts | 39-41 | `mungePath` 只替换 `/` | 同时替换 `/` 和 `\` |

### 🟡 Important（应该修复）

| # | 文件 | 行 | 问题 | 修复 |
|---|------|-----|------|------|
| 5 | thumbnail.rs | 28 | `/tmp` 回退路径 | `std::env::temp_dir()` |
| 6 | screenshot.rs | 77 | `/tmp` 回退路径 | `std::env::temp_dir()` |
| 7 | useProjectMemory.ts | 157,208 | 硬编码 `/Users/ylyg` | 移除或用空字符串 |
| 8 | workspace.ts | 103,202,266,326 | `split("/")` 仅 Unix | 处理 `/` 和 `\` |
| 9 | usePathResolver.ts | 44,70 | 相对路径假设 Unix | 添加 `\` 分隔符 |
| 10 | screenshot.rs | 84-88 | 截图命名模式 | 添加 Windows `screenshot (` |

---

# 三、实施计划

## Phase 1: Rust 后端跨平台（30 分钟）

```rust
// screenshot.rs — #[cfg] 分支
#[cfg(target_os = "macos")]
fn get_screenshot_dir() -> PathBuf {
    // defaults read com.apple.screencapture location
}

#[cfg(target_os = "windows")]
fn get_screenshot_dir() -> PathBuf {
    dirs::home_dir().unwrap().join("Pictures").join("Screenshots")
}

// disk.rs — 纯 Rust 替代 du -sk
fn run_du_sk(path: &Path) -> Result<u64, String> {
    let mut total = 0u64;
    for entry in walkdir::WalkDir::new(path) {
        if let Ok(entry) = entry {
            if entry.file_type().is_file() {
                total += entry.metadata().map(|m| m.len()).unwrap_or(0);
            }
        }
    }
    Ok(total / 1024)
}

// 所有 /tmp → std::env::temp_dir()
```

## Phase 2: 前端跨平台（20 分钟）

```typescript
// usePathResolver.ts — 添加 Windows 路径模式
const absolutePattern = /(?:\/[^\s:*?"<>|]+|[A-Z]:\\[^\s:*?"<>|]+)/g;

// workspace.ts — 通用路径分割
function fileName(path: string): string {
  return path.split(/[\\/]/).pop() || path;
}

// useProjectMemory.ts — 双分隔符 munge
function mungePath(dir: string): string {
  return dir.replace(/[\/\\]/g, '-').replace(/:/g, '');
}
```

## Phase 3: 构建配置（10 分钟）

```json
// tauri.conf.json — 已有 Windows 目标
"targets": ["nsis", "msi", "app", "dmg"]
// 已有 icon.ico — 无需修改
```

---

# 四、关键代码片段

## 4.1 跨平台截图目录

```rust
use std::path::PathBuf;

fn get_screenshot_dir() -> PathBuf {
    #[cfg(target_os = "macos")]
    {
        std::process::Command::new("defaults")
            .args(["read", "com.apple.screencapture", "location"])
            .output()
            .ok()
            .and_then(|o| {
                let s = String::from_utf8_lossy(&o.stdout).trim().to_string();
                if s.is_empty() { None } else { Some(PathBuf::from(s)) }
            })
            .unwrap_or_else(|| dirs::home_dir().unwrap_or_else(|| PathBuf::from(".")).join("Desktop"))
    }

    #[cfg(target_os = "windows")]
    {
        // Windows 10+ 截图默认保存到 Pictures\Screenshots
        // Win+Shift+S 保存到 OneDrive\Pictures\Screenshots (如果 OneDrive 启用)
        let pictures = dirs::picture_dir().unwrap_or_else(|| dirs::home_dir().unwrap().join("Pictures"));
        let screenshots = pictures.join("Screenshots");
        if screenshots.exists() { screenshots } else { pictures }
    }

    #[cfg(not(any(target_os = "macos", target_os = "windows")))]
    {
        dirs::desktop_dir().unwrap_or_else(|| dirs::home_dir().unwrap().join("Desktop"))
    }
}
```

## 4.2 跨平台目录大小

```rust
fn dir_size_kb(path: &Path) -> Result<u64, String> {
    let mut total = 0u64;
    let mut stack = vec![path.to_path_buf()];
    while let Some(dir) = stack.pop() {
        let read = std::fs::read_dir(&dir).map_err(|e| e.to_string())?;
        for entry in read.flatten() {
            let p = entry.path();
            if p.is_dir() {
                stack.push(p);
            } else if p.is_file() {
                total += entry.metadata().map(|m| m.len()).unwrap_or(0);
            }
        }
    }
    Ok(total / 1024)
}
```

## 4.3 跨平台路径分割（前端）

```typescript
// 统一路径处理工具
export function getFileName(path: string): string {
  return path.split(/[\\/]/).pop() || path;
}

export function getParentDir(path: string): string {
  const parts = path.split(/[\\/]/);
  parts.pop();
  return parts.join('/') || '/';
}

export function isAbsolute(path: string): boolean {
  return /^\//.test(path) || /^[A-Z]:\\/i.test(path);
}
```
